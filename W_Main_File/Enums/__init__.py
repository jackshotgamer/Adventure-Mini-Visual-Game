import enum

class Terrains(enum.Enum):
    plains = enum.auto()
    p_trap = enum.auto()
    forest = enum.auto()
    f_trap = enum.auto()
    mountn = enum.auto()
    vilage = enum.auto()
    p_home = enum.auto()
    # plains
    # plains trap
    # forest
    # forest trap
    # mountain
    # village
    # player home
